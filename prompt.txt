Create a production-valid Terraform configuration for AWS EKS with:

* EKS Auto Mode NOT enabled
* public EKS API endpoint enabled
* managed node group using m5.4xlarge
* node group max size = 5
* minimum 2 public subnets across different AZs
* VPC, IGW, route tables, subnet associations
* required IAM roles AND policy attachments
* security groups
* provider.tf and versions.tf
* outputs.tf
* tags required for Kubernetes LoadBalancer support
* Terraform AWS provider v5+
* code must pass terraform validate
* code should be deployable without modification

------------
# AKS CLUSTER 

Create a production-valid Terraform configuration for Azure AKS with:

* AKS private cluster NOT enabled
* public Kubernetes API server endpoint enabled
* system node pool using Standard_D16s_v5 VM size
* node pool autoscaling enabled
* minimum node count = 2
* maximum node count = 5
* minimum 2 subnets across different availability zones
* Virtual Network (VNet)
* subnets
* route tables
* NSGs (Network Security Groups)
* subnet associations
* NAT Gateway for outbound internet access
* Azure Container Registry (ACR)
* managed identity enabled
* required role assignments for AKS to pull images from ACR
* Azure CNI networking
* RBAC enabled
* OIDC issuer enabled
* workload identity enabled
* Log Analytics Workspace
* Azure Monitor Container Insights enabled
* provider.tf and versions.tf
* variables.tf
* outputs.tf
* Terraform azurerm provider v4+
* code must pass terraform validate
* code should be deployable without modification
* use best practices for production AKS
* include tags on all resources
* avoid deprecated Terraform arguments
* use latest stable AKS version available from data source
* expose kubeconfig output securely
* configure node resource group automatically
* include lifecycle and dependency handling where required
* generate complete modular Terraform structure
* ensure compatibility with Terraform 1.6+
